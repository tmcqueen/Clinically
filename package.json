{
  "name": "practice-management-app",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "bun --cwd frontend dev & bun --cwd backend dev",
    "dev:frontend": "bun --cwd frontend dev",
    "dev:backend": "bun --cwd backend dev",
    "deploy:backend": "bun --cwd backend deploy"
  }
}
